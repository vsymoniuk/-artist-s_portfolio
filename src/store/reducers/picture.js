const initialState = {
    pictures: []
}

export default function pictureReducer(state = initialState, action) {
    switch (action.type) {
        
        default:
            return state;
    }
}